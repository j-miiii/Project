-- ============================================================
-- infusions 테이블 기존 레코드 비활성화
-- 새 수액 데이터 추가 전, 기존 수액을 is_active = 0으로 변경
-- ============================================================
UPDATE `infusions`
SET `is_active` = 0
WHERE `is_active` = 1;
